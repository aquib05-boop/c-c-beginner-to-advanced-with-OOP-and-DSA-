#include <stdio.h>

int main(){
    int r = 5;
    int h = 8;
    printf("the area of the circle with radius %d is %f", r , 3.14*r*r);
    printf("\nthe volume of the cylinder of height %d & radius %d is %f",h , r,  3.14*r*r*h);
    return 0;
}