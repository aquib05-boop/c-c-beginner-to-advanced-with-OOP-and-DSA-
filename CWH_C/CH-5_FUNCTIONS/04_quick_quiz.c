#include <stdio.h>
#include <math.h>

int main(){
    int a=5;                                           //%d gives 0 as output
    printf("the are of this square is %f\n", pow(a,2));//gives double data type, so %f is used
    return 0;
}