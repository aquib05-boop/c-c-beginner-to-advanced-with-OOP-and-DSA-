#include <stdio.h>

int main(){
    int marks[90];

    marks[0]=83;
    marks[1]=9;
    printf(" marks 0 and marks 1 is %d, %d", marks[0], marks[1]);
    return 0;
    //  marks can be stroed from 0 to 89,i.e, 90 elements
    // contigious memory locations
}