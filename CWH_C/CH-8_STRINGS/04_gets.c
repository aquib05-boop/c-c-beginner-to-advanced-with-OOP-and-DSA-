#include <stdio.h>

int main(){
    char st[30];
    gets(st); //the entered string is stored in st!


    // printf("%s", st);

    // puts(st); //prints and places teh crusor to the next lines
    // printf("%s", st);
    printf("%s", st);
    return 0;
}