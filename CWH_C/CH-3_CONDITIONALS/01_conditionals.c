#include <stdio.h>

 int main(){
    int age = 18;

    if(age>10){
        printf("Your age is greater than 10");
    }
    return 0;
//If single line is there inside if,then curly brackets can also be absent
//and the outcome would be the same 
 }