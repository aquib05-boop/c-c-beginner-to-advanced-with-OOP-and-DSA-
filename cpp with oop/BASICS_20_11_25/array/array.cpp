#include <bits/stdc++.h>
using namespace std;

int main(){
    string s = "Striver";
    int len = s.size();
    cout<<"Length if the string: "<<len << endl;
    cout << s[2] << endl;
    
    s[2] = 'a';
    cout<< s;
    return 0;
}
    // // storeed in a memory location throughout the cpu in consecutive memory address

    // int arr[5];
    // cin >> arr[0] >> arr[1] >> arr[2] >> arr[3] >> arr[4] >> arr[5];
    
    // cout << arr[3];